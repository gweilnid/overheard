node_modules/
.next/
.env
.env.local
.DS_Store
dist/
.claude/

# Vendored HyperFrames agent skills — reinstall with: npx skills add heygen-com/hyperframes
# (skills-lock.json is committed; the 21MB of skill bodies is not)
.agents/

# Render review artifacts, regenerated by: npx hyperframes snapshot
videos/*/snapshots/
